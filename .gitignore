# Python
__pycache__/
*.py[cod]
backend/.venv*/
.venv*/

# Data (regenerate with scripts/download.py + scripts/ingest.py)
backend/data/zolpo.db
backend/data/dumps/

# OS
.DS_Store
*.db.bak
*.db-journal
logging.log

# User data — NEVER commit (emails, sessions, behavioural events)
backend/data/users.db*

# Local DB snapshots — too large for git, and users.db is personal data
backups/
