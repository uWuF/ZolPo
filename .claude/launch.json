{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "zolpo",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "exec backend/.venv312/bin/python -m uvicorn main:app --app-dir backend --host 127.0.0.1 --port ${PORT:-8020}"],
      "autoPort": true
    }
  ]
}
